namespace Blast.Bot.Observation
{
    /// <summary>
    /// Defines whether a bot observes the player-visible game state or the full internal state.
    /// </summary>
    public enum ObservationMode { Fair, Oracle }
}
